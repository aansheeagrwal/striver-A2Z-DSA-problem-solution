class Solution {
  public:
    Node* constructDLL(vector<int>& arr) {
        int n=arr.size();
        for(int i=0;i<n;i++){
            Node*temp=new Node(arr[i]);
            cout<<temp->data<<" ";
        }
    }
};

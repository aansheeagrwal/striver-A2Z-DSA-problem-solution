void MyQueue::push(int x) {
    if (rear < 99999) {  
        rear++;
        arr[rear] = x;
    }
}

int MyQueue::pop() {
    if (front > rear) {
        return -1;  
    }
    return arr[front++];
}
